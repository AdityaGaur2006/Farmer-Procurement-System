{
  "name": "kisansetu",
  "version": "0.1.0",
  "private": true,
  "description": "Farmer procurement prototype API and web client",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "check": "node --check server.js && node --check app.js && node --check pages.js"
  },
  "engines": { "node": ">=20" }
}
